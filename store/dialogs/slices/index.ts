import { addRoomSlice } from "./add-room-slice";
import { addSubjectSlice } from "./add-subject-slice";
import { addTeacherSlice } from "./add-teacher-slice";

export { addSubjectSlice, addRoomSlice, addTeacherSlice };
