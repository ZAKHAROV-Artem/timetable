import { BookOpen, Home, List, Settings, UserRound } from "lucide-react-native";
import { iconWithClassName } from "@/lib/utils";
iconWithClassName([Home, UserRound, List, BookOpen, Settings]);
export { Home, UserRound, List, BookOpen, Settings };
